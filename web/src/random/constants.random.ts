import type { GenConfig } from "./types.random"

export const DEFAULT_CONFIG: GenConfig = {
    x0: 13,
    m: 67108865,
    a: 17,
    c: 1,
}

export const DEFAULT_N = 10
export const MIN_N = 1
export const MAX_GENERATE_N = 9999
